return {
  "akinsho/bufferline.nvim",
  dependencies = { "nvim-tree/nvim-web-devicons" },
 -- version = "*",  -- temp commented until get a better way to resolve a bug with not competable with nvim 10
  opts = {
    options = {
      mode = "tabs",
      separator_style = "slant",
    },
  },
}
